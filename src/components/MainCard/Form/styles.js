import { makeStyles } from "@material-ui/core";

export default makeStyles(() => ({
  button: {
    marginTop: "1.25rem",
  },
  radioGroup: {
    display: "flex",
    justifyContent: "center",
    marginBottom: "-0.6rem",
  },
}));
